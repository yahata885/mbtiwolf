<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".Title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ModeExplanation_Activity" />
        <activity android:name=".Creditactivity" />
        <activity android:name=".SetupActivity"
                    android:exported="false"
                    android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Mode1InputActivity" />
        <activity android:name=".Mode23InputActivity" />
        <activity android:name=".DiscussionActivity" />
        <activity android:name=".ResultMode1Activity" />
        <activity android:name=".ResultMode2Activity" />
        <activity android:name=".ResultMode3Activity" />
        <activity android:name=".AnswerInputActivity" />
    </application>

</manifest>